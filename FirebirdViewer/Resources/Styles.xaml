<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== Color palette ===== -->
    <SolidColorBrush x:Key="PanelBackground" Color="#F4F4F6"/>
    <SolidColorBrush x:Key="PanelBorder" Color="#D8D8DD"/>
    <SolidColorBrush x:Key="HeaderBackground" Color="#EEEFF3"/>
    <SolidColorBrush x:Key="AltRowBackground" Color="#F7F7FA"/>
    <SolidColorBrush x:Key="StatusBackground" Color="#ECECEF"/>
    <SolidColorBrush x:Key="AccentForeground" Color="#1C5DB0"/>
    <SolidColorBrush x:Key="MutedForeground" Color="#666666"/>

    <!-- ===== TextBox ===== -->
    <Style TargetType="TextBox">
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="BorderBrush" Value="{StaticResource PanelBorder}"/>
    </Style>

    <Style TargetType="PasswordBox">
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="BorderBrush" Value="{StaticResource PanelBorder}"/>
    </Style>

    <!-- ===== Buttons ===== -->
    <Style TargetType="Button">
        <Setter Property="Padding" Value="14,5"/>
        <Setter Property="MinHeight" Value="28"/>
    </Style>

    <Style x:Key="LinkButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource AccentForeground}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="4,2"/>
    </Style>

    <!-- ===== Section headers ===== -->
    <Style x:Key="SectionHeader" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource HeaderBackground}"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="BorderBrush" Value="{StaticResource PanelBorder}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>

    <Style x:Key="SectionHeaderText" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <!-- ===== ListBox (for table list) ===== -->
    <Style TargetType="ListBox">
        <Setter Property="BorderBrush" Value="{StaticResource PanelBorder}"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
    </Style>

    <!-- ===== DataGrid ===== -->
    <Style TargetType="DataGrid">
        <Setter Property="AutoGenerateColumns" Value="True"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="HeadersVisibility" Value="All"/>
        <Setter Property="GridLinesVisibility" Value="All"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource AltRowBackground}"/>
        <Setter Property="EnableRowVirtualization" Value="True"/>
        <Setter Property="EnableColumnVirtualization" Value="True"/>
        <Setter Property="RowHeaderWidth" Value="22"/>
        <Setter Property="HorizontalGridLinesBrush" Value="#E5E5EA"/>
        <Setter Property="VerticalGridLinesBrush" Value="#E5E5EA"/>
        <Setter Property="BorderBrush" Value="{StaticResource PanelBorder}"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource HeaderBackground}"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="BorderBrush" Value="{StaticResource PanelBorder}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
    </Style>

    <!-- ===== TabControl ===== -->
    <Style TargetType="TabItem">
        <Setter Property="Padding" Value="14,6"/>
    </Style>

</ResourceDictionary>
